(define _iter (lambda (fn l i)
    (if (< i (len l))
        (do
            (fn (nth l i))
            (_iter fn l (+ i 1)))
        null)))

(define iter (lambda (fn l) (_iter fn l 0)))

(iter print (list "hello" "world"))

